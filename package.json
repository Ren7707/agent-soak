{
  "name": "agent-soak",
  "version": "0.1.0",
  "description": "AI-ready universal soak testing for CLI tools and web agents",
  "type": "module",
  "files": ["src", "types", "templates", "platform.manifest.schema.json", "contract.schema.json", "test-plan.schema.json", "README.md", "LICENSE"],
  "types": "types/index.d.ts",
  "bin": {
    "agent-soak": "src/cli.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/cli.js && node --check src/runner/run.js && node --check src/artifacts/index.js && node --check src/plans/scaffold.js && node --check src/reports/compare.js && node -e \"for (const f of ['platform.manifest.schema.json','contract.schema.json','test-plan.schema.json']) JSON.parse(require('fs').readFileSync(f,'utf8')); console.log('schemas ok')\" && git diff --check",
    "demo": "node examples/demo-platform/server.js",
    "inspect": "node src/cli.js inspect",
    "analyze": "node src/cli.js analyze --json",
    "contract": "node src/cli.js contract --json",
    "validate": "node src/cli.js validate",
    "run": "node src/cli.js run",
    "cleanup": "node src/cli.js cleanup",
    "residue": "node src/cli.js residue"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "playwright": "^1.62.1",
    "yaml": "^2.9.0"
  }
}
